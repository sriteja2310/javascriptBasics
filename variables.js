/* 3 ways to declare a variable
1. var - used throughout the program
2. const - is a variable which can never be changed
3. let - used within the defines scope

Javascript is case sensitive language
Using camel case sensitivity is a good practice 
*/
var empName = 'Kaira';

let companyName = 'candid';

const pi = 3.14

/*
DECLARING AND INITIALIZING VARIABLES 
*/

var a; //declaring a variable
console.log(typeof a); // if a variable is not initialized the datatype of the variable will be undefined

var b = 2; //assigning a value to a variable

console.log(b);
